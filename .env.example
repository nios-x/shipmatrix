# Origin of the ShipMatrix payments server (the `shipmatrix-server` project).
# Leave unset to use the production host baked into src/lib/api.ts.
# For a device on the same Wi-Fi, use your machine's LAN IP — not localhost,
# which on a phone points at the phone itself.
EXPO_PUBLIC_PAYMENTS_URL=http://172.26.75.194:8080
 